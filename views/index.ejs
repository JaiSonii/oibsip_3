<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Temperature Converter</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&display=swap" rel="stylesheet">
</head>
<body>
    
    <section id="temp"<% if(locals.con){ if((con == "°C" && parseInt(answer) >=45)||(con =="K" && parseInt(answer) >= 318)||(con == "F" && parseInt(answer)>=113) ){%>
        class="hot"
    <%} else if((con == "°C" && parseInt(answer) >=20)||(con =="K" && parseInt(answer) >= 293)||(con == "F" && parseInt(answer)>=68) ){ %>
        class="normal"
    <% } else if((con == "°C" && parseInt(answer) <20)||(con =="K" && parseInt(answer) <293)||(con == "F" && parseInt(answer) < 68)) { %>
        class="cold"
    <% }  } %> >
    
    <div class="container d-flex flex-column align-items-center justify-content-center main-box px-4 py-0">
        <div class="container px-4 pt-5 fs-1"><p class="text-center heading">Temperature Converter 🌡️</p></div>
        <form class="d-flex flex-column align-items-center justify-content-evenly w-50 px-4 py-0" action="/convert" method="post">
        <div class="d-flex justify-content-around w-100 fs-3">
            <label for="degree">QUANTITY: </label>
            <input class="all-box" type="text" name="degree" id="">
        </div>

    <div class="d-flex justify-content-around fs-4 w-100">
        <div >
            <label class="fs-3" for="from">FROM: </label>
            <select class="all-box text-center" name="from" id="from">
                <option value="none" selected disabled hidden>NONE</option>
                <option value="°C">CELCIUS</option>
                <option value="F">FAHRENHEIT</option>
                <option value="K">KELVIN</option>
            </select>
        </div>

        <div>
            <label class="fs-3" for="to">TO: </label>
            <select class="all-box text-center" name="to" id="to">
                <option value="none" selected disabled hidden>NONE</option>
                <option value="°C">CELCIUS</option>
                <option value="F">FAHRENHEIT</option>
                <option value="K">KELVIN</option>
            </select>
        </div>
    </div>

        <div class="fs-3 w-100 align-items-center">
            <input class="w-100 all-box" type="submit" value="SUBMIT">
        </div>

        <% if(locals.answer){ %>
            <div class="ans fs-3"><p><%= answer %></p></div>
        <% } %>
    </form>

    
        
    
</div>
<footer class="text-center">
    Made by J💕
</footer></section>




<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>

</body>
</html>